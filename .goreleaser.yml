---
version: 2
before:
  hooks:
    - go mod tidy
    - go mod verify
    - go vet
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64

archives:
  - files:
      - LICENSE
    builds_info:
      group: root
      owner: root
      mode: 0755
    name_template: "dcjulian29.apps.disk-usage_{{ .Version }}_{{ .Os }}"
    format_overrides:
      - formats: [ "zip" ]
        goos: windows

nfpms:
  - package_name: dcjulian29.apps.disk-usage
    homepage: https://github.com/dcjulian29/disk-usage/
    maintainer: Julian Easterling
    license: Apache 2.0
    description: |-
      disk-usage reports back what each directory consumes in space.
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin

chocolateys:
  - name: dcjulian29.apps.disk-usage
    authors: Julian Easterling
    project_url: https://github.com/dcjulian29/disk-usage/
    copyright: 2026 Julian Easterling
    license_url: https://github.com/dcjulian29/disk-usage/blob/main/LICENSE
    require_license_acceptance: false
    bug_tracker_url: https://github.com/dcjulian29/cli-tools/issues
    description: |
      disk-usage reports back what each directory consumes in space.
    api_key: "{{ .Env.CHOCO_API_KEY}}"
    source_repo: "https://www.myget.org/F/dcjulian29-chocolatey/"
    skip_publish: false
checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^admin:'
      - '^docs:'
      - '^test:'
